# Copiar como .env.local y ajustar si el API no corre en localhost:8080.
# Vacio = usa rutas relativas /api (funciona con el proxy de "npm run dev"
# o si el frontend se sirve detras del mismo dominio/reverse proxy que el API).
VITE_API_BASE_URL=
